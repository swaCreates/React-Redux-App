
const initialState= {
    quote: null,
    isFetching: false,
    error: '',
}

export const quoteReducer= (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}